import PropTypes from 'prop-types';
import React, { Fragment } from 'react';


/**
 * The homepage partial component
 */
const Homepage = ({ _body }) => (
	<Fragment>{ _body }</Fragment>
);

Homepage.propTypes = {};

Homepage.defaultProps = {};

export default Homepage;
