import PropTypes from 'prop-types';
import React from 'react';


/**
 * The footer partial component
 */
const Footer = ({ _body }) => (
    <footer>
        { _body }
    </footer>
);

Footer.propTypes = {};

Footer.defaultProps = {};

export default Footer;
